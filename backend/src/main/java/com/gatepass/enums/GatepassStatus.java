package com.gatepass.enums;

public enum GatepassStatus {
    PENDING,
    COORDINATOR_APPROVED,
    COORDINATOR_REJECTED,
    WARDEN_APPROVED,
    WARDEN_REJECTED,
    ACTIVE,
    COMPLETED,
    OVERDUE,
    CANCELLED
}